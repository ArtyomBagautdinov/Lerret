import React from "react";

const Lerret : React.FC = () => {

    return (
        <canvas className="w-screen h-screen border border-black" ref="lerret" width="400" height="400"></canvas>
    );
}

export default Lerret;